defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

collections:
  tags:
    output: true
    permalink: tags/:path/

plugins:
  - jekyll-feed
  - jekyll-seo-tag

url: https://2dcic.github.io
domain: 2dcic.github.io


future: true
